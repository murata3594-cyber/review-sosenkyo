# CLAUDE.md

Claude Code entrypoint for レビュー総選挙.

Read first:
- `DESIGN_SYSTEM.md`
- `IMPLEMENTATION_HANDOFF.md`
- `AGENTS.md`

Use the same repository rules as Codex. Never add secrets/private raw review data. Run `python scripts/validate_site.py` after edits. Keep `main` deployable.
