# Security policy

Do not commit credentials, API keys, cookies, tokens, `.env` files, personal data, or private raw review datasets to this repository.

If a secret is committed accidentally, revoke or rotate it immediately before removing it from Git history.
